{
  "name": "serverless-yaml-local-cloudformation-plugin",
  "version": "0.0.1",
  "description": "Use YAML instead of JSON for CloudFormation templates generated by the Serverless Framework.",
  "main": "index.js",
  "scripts": {
    "deploy": "publish",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neverendingqs/serverless-yaml-local-cloudformation-plugin.git"
  },
  "keywords": [
    "serverless",
    "framework",
    "aws",
    "cloudformation"
  ],
  "author": "neverendingqs",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/neverendingqs/serverless-yaml-local-cloudformation-plugin/issues"
  },
  "homepage": "https://github.com/neverendingqs/serverless-yaml-local-cloudformation-plugin#readme",
  "devDependencies": {
    "publish-me-maybe": "^1.0.0"
  }
}
